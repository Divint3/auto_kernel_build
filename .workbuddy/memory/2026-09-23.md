# 2026-09-23

- 创建 aarch64 内核编译项目（D:\Workspace\qemu）
- .github/workflows/build-kernel.yml：GitHub Actions 交叉编译 aarch64 内核
  - 自动下载最新 stable 内核（kernel.org releases.json）或指定版本
  - 指定版本 Arm GNU 工具链（aarch64-none-linux-gnu，默认 13.2.rel1）
  - 编译 vmlinux/Image/modules/dtbs；树外模块示例（kernel-module/）
  - 用户要求产物 QEMU 可引导：增加 busybox 静态交叉编译、initramfs.cpio.gz、rootfs.ext4（mkfs.ext4 -d 直接生成，含 /lib/modules 模块）、CI 内 qemu-system-aarch64 virt 机型启动自测（QEMU-BOOT-OK 标志）
  - scripts/run-qemu.sh：本地启动脚本，支持 ext4/initramfs/gdb(-m) 三种模式
- 用户场景：QEMU 虚拟化（工作目录名 qemu），需要可引导完整系统而非仅内核镜像
